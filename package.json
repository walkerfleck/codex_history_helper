{
  "name": "codex-history-helper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:frontend": "esbuild frontend/src/main.jsx --bundle --format=esm --outfile=frontend/dist/app.js --loader:.js=jsx --loader:.jsx=jsx",
    "start": "npm run build:frontend && node backend/src/server.js",
    "dev": "npm run build:frontend && node backend/src/server.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.8.1",
    "express": "^4.21.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
